services:
  - type: web
    name: skynet-ds
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: HF_API_KEY
        sync: false